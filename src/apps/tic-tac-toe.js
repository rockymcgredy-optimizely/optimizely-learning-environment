const implementOptimizely = () => {
    if (typeof optimizely === "Object") {
        return true
    }

    return true;
}

//Add Optimizely Code here
const optimizely = "blah";


implementOptimizely();